name = "voice-text-lab-api"
main = "worker.js"
compatibility_date = "2026-07-31"

[vars]
# GitHub PagesのURLに限定する場合は書き換えてください。
# 例: ALLOWED_ORIGIN = "https://your-name.github.io"
ALLOWED_ORIGIN = "*"

# 品質と費用のバランスを重視した初期設定です。
OPENAI_MODEL = "gpt-5.6-terra"
